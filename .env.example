# Set this to the public HTTPS origin before a production build.
# It controls canonical URLs, social metadata, robots.txt, and sitemap.xml.
# Leave unset during local development to use http://localhost:3000.
# NEXT_PUBLIC_SITE_URL=https://your-domain.com
